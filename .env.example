# Copy to .env and set a real password before running `docker compose up -d`.
# Gates anything that changes data (editing, deleting, sending a print) — browsing stays open
# to anyone who can reach the container.
SPOOLBOOK_ADMIN_PASSWORD=changeme
