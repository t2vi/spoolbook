<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="using:Spoolbook.Desktop.Common"
             xmlns:local="using:Spoolbook.Desktop.Controls"
             x:Class="Spoolbook.Desktop.Controls.Pagination"
             x:DataType="local:Pagination"
             x:Name="Root">
    <StackPanel Orientation="Horizontal" Spacing="8">
        <Button Content="Previous" Command="{Binding #Root.PreviousCommand}"/>
        <TextBlock Text="{Binding #Root.Page}" VerticalAlignment="Center"/>
        <TextBlock Text="/" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding #Root.TotalPages}" VerticalAlignment="Center"/>
        <Button Content="Next" Command="{Binding #Root.NextCommand}"/>
        <TextBlock Text="Per page" VerticalAlignment="Center" Margin="12,0,0,0"/>
        <ComboBox ItemsSource="{x:Static common:PageSizes.Options}" SelectedItem="{Binding #Root.PageSize}"/>
    </StackPanel>
</UserControl>
